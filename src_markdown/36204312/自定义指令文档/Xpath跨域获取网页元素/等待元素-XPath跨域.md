# 描述
Xpath跨域等待元素

![](https://cdn.nlark.com/yuque/0/2024/png/22242030/1721718270302-7864b410-15ee-4b6b-80a9-d18353df7ae8.png)



# 参数配置
## 指令输入
### 常规
**IFrame对象：**通过【初始化IFrame】指令或者【切换IFrame】指令获取的输出对象，也可以传入网页对象

**XPath：**目标元素的XPath，支持传入数组，可以实现精确切入多层IFrame查找元素。传入数组，则不会全局查找

**等待状态：**下拉框

+ 等待元素出现
+ 等待元素消失

**基于当前iframe全局查找：**

**勾选：**会基于当前iframe对象以及所有子iframe对象查找元素

**不勾选：**只在当前iframe对象查找元素

**超时时间**: 整数, 等待时间,不填则一直等待





### 高级
无



## 指令输出
无





